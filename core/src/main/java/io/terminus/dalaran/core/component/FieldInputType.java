package io.terminus.dalaran.core.component;

public enum FieldInputType {
    Hidden, Auto, String, Integer, Select, Radio, CheckBox, Password, Connector, Model, Script, SQL, SubFlow, Service, ServiceOperation, Routes, Pipeline, Branches
}
